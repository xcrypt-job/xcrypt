use base qw(core);
use File::Spec;
use interactive;

#if ($#ARGV == -1) { die "Can't delete any job without its id\n";}
foreach my $id (@ARGV) {
#    my $id = $ARGV[0];
    &interactive::qdel($id);
    print "$id is deleted by user.\n";
}
