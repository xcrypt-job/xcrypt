#!/usr/bin/env perl

use strict;
use File::Spec;
use Getopt::Long qw(:config pass_through);

our %options = ();

GetOptions
    (\%options,
     'aborted',
    );

my $xcrfile;
if (defined $options{'aborted'}) {
    $xcrfile = 'stat_aborted.xcr';
} else {
    $xcrfile = 'stat.xcr';
}
system('xcrypt ' . File::Spec->catfile($ENV{XCRYPT}, 'bin', 'script', $xcrfile) . ' ' . join (' ', @ARGV));
