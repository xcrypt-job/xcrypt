#!/usr/bin/env perl

use strict;
use File::Spec;
use Getopt::Long;

our %options = ();

GetOptions
    (\%options,
     'all',
    );

my $xcrfile;
if (defined $options{'all'}) {
    $xcrfile = 'cancelall.xcr';
} else {
    $xcrfile = 'cancel.xcr';
}
system('xcrypt ' . File::Spec->catfile($ENV{XCRYPT}, 'bin', 'script', $xcrfile) . ' ' . join (' ', @ARGV));
