use base qw(limit core);

&limit::initialize(10);

%template = (
    'id' => 'job6',
#    'exe' => 'bin/fib',
    'exe' => 'echo ',
    'arg0' => '40',
    'arg1' => '> out0',
);

my @jobs = &prepare_submit(%template,
 'arg0@' => [31,32],
# 'arg1@' => ['> out1','> out2'],
 'RANGE0' => [5], 'RANGE2' => [2,7],  # 'arg0@' => '$_[0] + 20 + $_[2];',
    );
&sync(@jobs);

