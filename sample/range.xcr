use base qw(common core);

%template1 = (
    'id@' => sub { "job6_$_[0]_$_[2]" },
    'exe0@' => sub { 'bin/fib ' . ($_[0] + $_[2]) . " > out_$_[0]_$_[2]"},
    'RANGE0' => [20..22],
    'RANGE2' => [0,3],
#    'RANGES' => [[20..22],[0],[0,3]],
    'JS_queue' => 'gh10034',
    'JS_group' => 'gh10034',
#    'before' => sub { print ${$self->{'VALUES'}}[2], "\n"; }
#    'before' => sub { print $self->{'VALUE2'}, "\n"; }
    'before' => sub { print $_[0], "\n"; print $self->{exe0}, "\n"; }
);

my @jobs = &prepare(%template1);
&common::get_job_states_from_left_messages();
&submit(@jobs);
&common::get_job_states_from_left_messages();
&sync(@jobs);




















=comment

%template2 = (
    'id@' => sub { "job6_$_[0]_$_[1]"; },
#    'id' => 'job6',
    'exe' => 'bin/fib',
    'RANGE0' => [31,32],
    'RANGE1' => [0,2],
    'arg0@' => '$_[0] + $_[1]',
    'arg1@' => sub { "> out_$_[0]_$_[1]" }
);
&prepare_submit_sync(%template2);

=cut
