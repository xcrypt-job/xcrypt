# Sumit just a single job.
use base qw (core);

%template = (
    'id' => 'job0',
    'exe' => 'bin/fib-file dat/num40 out',
    'JS_queue' => 'gh10034',
    'JS_group' => 'gh10034',
    'before' => sub {
        print "Submitting $self->{id}\n";
    },
    'after' => sub {
        print "$self->{id} finished\n";
    } 
    );
@jobs=&prepare_submit_sync (%template);
