# Sumit just a single job.
use base qw (core);

%template = (
             'id' => 'job0',
             'exe' => 'bin/fib-file',
             'arg0' => "dat/num40",
             'arg1' => "output",
             'before_in_job' => 'print STDERR $self->{exe}. "\n";',
#             'before_in_job' => sub {print STDERR $self->{JS_queue}. "\n";},
             'after_in_job' => 'print STDOUT $self->{id}. "\n";',
             );
&prepare_submit_sync (%template);
