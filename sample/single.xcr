# Sumit just a single job.
use base qw (core);

%template = (
             'id' => 'example',
             'exe0' => './a.out',
             'arg0_0' => "input",
             'arg0_1' => "output",
             'copiedfile0' => 'a.out',
             'copiedfile1@' => '"input"',
             'JS_queue' => 'ghxxxxx',  # modify for your system
             'JS_group' => 'ghxxxxx',  # modify for your system
             );
@job = prepare (%template);
submit(@job);
sync(@job);
