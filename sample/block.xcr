use base qw(core);

%template = (
    'id' => 'job6',
    'RANGE0' => [40],
    'RANGE2' => [0],
);

my @jobs = &prepare(%template);
async_in_job {
    return 'bin/fib '. 40 ." > " . $_[0]->{id};
} @jobs;
&sync(@jobs);
