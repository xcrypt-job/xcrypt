use base qw(limit core);

&limit::initialize(10);

%template = (
    'id' => 'job6',
    'exe' => 'bin/fib',
    'RANGE0' => [31,32],
    'RANGE1' => [0,1],
    'arg0@' => sub { ($_[0] + $_[1]) },
    'arg1@' => sub { "> out$_[0]_$_[1]" }
);

&prepare_submit_sync(%template);

