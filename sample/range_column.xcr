use base qw(limit core);

&limit::initialize(10);

%template = (
    'id' => 'job6',
    'exe@' => sub { 'bin/fib ' . ($_[0] + $_[1]) . " > out_$_[0]_$_[1]"},
    'RANGE0' => [31,32],
    'RANGE1' => [0,1]
);
&prepare_submit_sync(%template);

