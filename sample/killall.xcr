use base qw(core);

%xyz = (
    'id' => 'job100',
    'exe' => './kempo',
    'arg0' => 'plasma.inp',
    'linkedfile0' => 'kempo',
    'copiedfile0' => 'plasma.inp',
    'queue' => 'gh10034',
    'option' => '# @$-g gh10034'
);

# stdoutキーはSGEでのみ正常に動作する
my @jobs = &prepare(%xyz, 'RANGE0' => [0..2], 'arg1S' => [0..2],
 'after' => 'eval(if ($self->{stdout} == 11) { system("qdel -u username"); })');
my @thrds = &submit(@jobs);
my @results  = &sync(@thrds);
