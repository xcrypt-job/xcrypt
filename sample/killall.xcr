use base qw(core);

%xyz = (
    'id' => 'job6',
    'queue' => 'gh10034',
    'option' => '# @$-g gh10034'
);

&prepare_submit_sync(%xyz,
#my @jobs = &prepare(%xyz,
		     'RANGE0' => [0..3],
		     'exeS' => sub { "sleep $_[0]; echo $_[0]"; },
# 'after' => 'if ($self->{id} eq "job6_2") { system("xcryptdel --all"); }'
		     'after' => 'print STDOUT "hoge.\n";'
);
#&submit_sync(@jobs);
