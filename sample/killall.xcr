use base qw(core);

%xyz = (
    'id' => 'job6',
    'exe' => 'echo',
    'queue' => 'gh10034',
    'option' => '# @$-g gh10034'
);

&prepare_submit_sync(%xyz, 'arg0S' => [0..9],
 'after' => 'if ($self->{id} eq "job6_2") { system("xcryptdel --all"); }'
);
